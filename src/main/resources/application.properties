spring.application.name=echodeck_backend_470sp25_team3

# Use Render's database environment variables, fallback to local if not set
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/echodeck}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:sesame}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# JWT Secret
jwt.secret=79rHtObqJdjaRjKv3GVmMqJqHqWI5wM6N7++eksg+bA=